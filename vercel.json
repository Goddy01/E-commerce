{
    "builds": [{
        "src": "E_commerce/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "E_commerce/wsgi.py"
        }
    ]
}